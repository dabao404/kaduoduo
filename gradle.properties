org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# 启用 AndroidX。项目依赖 Compose、Room、Lifecycle、Navigation 等 AndroidX 组件，必须开启。
android.useAndroidX=true

# 兼容仍引用旧 support 包命名的第三方依赖。当前项目未直接使用旧依赖，但开源项目保留该开关更稳妥。
android.enableJetifier=true

# Kotlin/Gradle 构建稳定性配置。
kotlin.code.style=official
